# Gate UI uses the Python standard library (tkinter, urllib, json) plus optional
# linux evdev for reliable USB QR-wedge input under Wayland (no window focus).
#
# On Raspberry Pi OS:
#   sudo apt install python3-tk python3-evdev mpg123
# (ffplay from ffmpeg also works if mpg123 is unavailable)
